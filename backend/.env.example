# CORS_ORIGINS=*   # default — allow all origins (quick fix for Vercel + local dev)
# CORS_ORIGINS=https://deepfake-detection-system-livid.vercel.app,http://localhost:3000
# Vercel preview URLs (*.vercel.app) are also allowed via origin regex in main.py

# MODEL_WEIGHTS_PATH=deepfake_efficientnet.pth
# MODEL_DEVICE=cuda
# PRELOAD_MODEL_ON_STARTUP=true
# MAX_IMAGE_UPLOAD_BYTES=10485760
# MAX_VIDEO_UPLOAD_BYTES=104857600
# VIDEO_MAX_FRAMES=32
# FACE_MODEL_PATH=
# FACE_SCORE_THRESHOLD=0.6
# FACE_CROP_PADDING_RATIO=0.2
