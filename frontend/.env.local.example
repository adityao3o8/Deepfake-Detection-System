# Local backend (default)
BACKEND_INTERNAL_URL=http://127.0.0.1:8000

# Vercel production — set this, then redeploy:
# BACKEND_INTERNAL_URL=https://deepfake-detection-system-2-2rgl.onrender.com
#
# Browser always calls /api/proxy/detect (same origin).
# Do NOT set NEXT_PUBLIC_API_URL on Vercel (causes direct cross-origin calls).
