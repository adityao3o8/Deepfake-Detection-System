# Optional: direct API URL (bypasses same-origin proxy). Leave unset for external deploy.
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-side proxy target (set when running on an external host)
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
